/* components/user-avatar/user-avatar.wxss - 统一头像组件样式 */
.user-avatar {
  display: block;
  object-fit: cover;
  background-color: #f5f5f5;
  border: 2rpx solid #fff;
  box-shadow: 0 4rpx 8rpx rgba(0, 0, 0, 0.1);
  transition: all 0.3s ease;
}

.user-avatar:active {
  transform: scale(0.95);
}

